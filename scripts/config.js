import { CanopyExtension } from 'lib/canopy/CanopyExtension';

const extension = new CanopyExtension({
    name: 'Your Extension Name',
    description: 'A new extension for §l§aCanopy§r!',
    version: '1.0.0',
});

export default extension;